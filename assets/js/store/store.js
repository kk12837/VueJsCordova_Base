/**
 * store.js is where the Vuex is initialized, with the required store modules passed into it.
 */
import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

export const store = new Vuex.Store({
    modules: {

    }
});
